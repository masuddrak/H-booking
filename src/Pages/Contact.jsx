import { Helmet } from "react-helmet";

const Contact = () => {
    return (
        <div>
             <Helmet>
                <title>Contact</title>
            </Helmet>
            <h1>contact section</h1>
        </div>
    );
};

export default Contact;